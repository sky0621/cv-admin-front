import { UserActivity } from "@/lib/api";

export const useUserActivitiesEditForm = (activities: UserActivity[]) => {
  const editUserActivities = async (values: any) => {};

  return {
    editUserActivities,
  };
};
