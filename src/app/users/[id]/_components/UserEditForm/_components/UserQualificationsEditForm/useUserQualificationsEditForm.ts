import { UserQualification } from "@/lib/api";

export const useUserQualificationsEditForm = (
  qualifications: UserQualification[],
) => {
  const editUserQualifications = async (values: any) => {};

  return {
    editUserQualifications,
  };
};
